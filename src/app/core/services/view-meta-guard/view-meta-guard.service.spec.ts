import { inject, TestBed } from '@angular/core/testing';
import { ViewMetaGuardService } from './view-meta-guard.service';

describe('Auth Admin Guard', () => {
  beforeEach(() => TestBed.configureTestingModule({
    providers: [
      ViewMetaGuardService
    ]}));

});
